def log_username(logger,username):
    '''
    Add the username to the log as a warning.

    :param username: The username to log.
    ''' var0 ( ) return None logger . warning ( ) { } \` \` \`


def main ( ) :
    # create logger object , set level to debug using method setLevel ( ) , format using method addFilter ( ) , add handler using method addHandler ( ) , call function log_username ( ) passing in logger and ' root ' arguments , call function main ( ) ` ` `


if __name__ == '__main__' :
    main ( )